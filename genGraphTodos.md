gen graph todos, some items might require modifying pathux.

[ ]: split the gen graph header into two rows
[ ]: the gen graph editor's header should show the name of the asset referencing the shot
[ ]: gen graph editor should have a 'regenerate' button that runs the pipeline
[x]: model selection props in the gen graph node editor should use a dropdown box.
