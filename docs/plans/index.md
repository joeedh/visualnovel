# Plan index and status

Every implementation plan in this directory, and whether it has been built. A plan is the
authority on its own scope; this file is the authority on **what state it is in**.

Status values:

- **shipped** — implemented and in the codebase. The plan's own As-shipped section records
  deviations; the plan is history, not a task list.
- **partial** — some of it is built and the plan still describes unbuilt work.
- **planned** — nothing built yet.
- **superseded** — a later plan took the work over. The row names the plan that did.

**A shipped plan's file lives in [`archive/`](archive)**; open plans and the tracker files
stay in this directory. This table indexes both, and moving a file the moment its row flips
to shipped is part of flipping the row (see
[`../reference/conventions.md`](../reference/conventions.md#plans)).

Three batches carry extra working detail of their own. Two are complete:
[`desktop-editors-tracking.md`](desktop-editors-tracking.md) for the desktop editors, and the
[scene authoring](#scene-authoring) section below. The third is the eight open plans of the
authoring surface, whose running order and checkboxes are in
[authoring-surface-tasklist](archive/INDEX.md#authoring-surface-tasklist).

<!-- toc -->

<!-- tocstop -->

## All plans

| Plan | Status | What it covers |
| --- | --- | --- |
| [`archive/INDEX.md#initial-implementation`](archive/INDEX.md#initial-implementation) | shipped | The monorepo, package layering, phases P1–P7, task graph, store, scheduler, `vngen` CLI |
| [`archive/INDEX.md#authoring-agent-implementation`](archive/INDEX.md#authoring-agent-implementation) | shipped | `vnauthor`: workspace index, tool registry, plan/execute modes, skills, the REPL |
| [`archive/INDEX.md#runner`](archive/INDEX.md#runner) | shipped | `story.play.json`, `@vn/export`, the desktop app and its PLAY room |
| [`archive/INDEX.md#test-fixtures`](archive/INDEX.md#test-fixtures) | shipped | `@vn/testkit` — real projects on disk run through the real scheduler |
| [`archive/INDEX.md#desktop-renderer-restructure`](archive/INDEX.md#desktop-renderer-restructure) | shipped | One directory per room, pure `.ts` cores with `tests/` siblings, the stylesheet split |
| [`archive/INDEX.md#refine-loop-inspector`](archive/INDEX.md#refine-loop-inspector) | shipped | The FLOOR inspector's rendering of the P7 generate→critique→refine loop |
| [`archive/INDEX.md#story-branch-editor`](archive/INDEX.md#story-branch-editor) | shipped | STUDIO's `branches` mode, `renderer/graph/`, the semantic drag gestures |
| [`archive/INDEX.md#task-dag-view`](archive/INDEX.md#task-dag-view) | shipped | FLOOR's `graph` mode: barrier nodes, ref edges, ghosts |
| [`archive/INDEX.md#shot-timeline-editor`](archive/INDEX.md#shot-timeline-editor) | shipped | FLOOR's `timeline` mode and `story.setCoverage` |
| [`archive/INDEX.md#command-system`](archive/INDEX.md#command-system) | shipped | `@vn/commands`, the DSL, the catalog, provenance, CDP |
| [`archive/INDEX.md#command-undo-redo`](archive/INDEX.md#command-undo-redo) | shipped | Opt-in undo via shadow snapshots under `refs/vn/undo/<seq>` |
| [`archive/undo-refactor.md`](archive/undo-refactor.md) | shipped | Undo off git and onto an in-memory content-addressed store, plus the desktop file cache it shares |
| [`archive/undo-checkpoints.md`](archive/undo-checkpoints.md) | shipped | `CommandStack.beginCheckpoint`/`endCheckpoint` group several commands into one undo point, scoped to a declared subtree with automatic rollback on failure; `apps/desktop`'s `GenGraphEditor` opens one around a multi-node delete/duplicate, through a `vendor/path.ux` `NodeGraphDelegate` widened to genuinely async `undoStepBegin`/`undoStepEnd` |
| [`archive/INDEX.md#preconditions-and-timeline-interaction`](archive/INDEX.md#preconditions-and-timeline-interaction) | shipped | `Command.check`, `stack.check`'s three states, the `timeline.cover` interaction |
| [desktop-storage-and-draggable-rail](archive/INDEX.md#desktop-storage-and-draggable-rail) | shipped | `.vndesktop/session.json`, `usePanelWidth`, `view.panelSize` |
| [`archive/INDEX.md#sample-workspace-and-asset-cache`](archive/INDEX.md#sample-workspace-and-asset-cache) | shipped | `examples/mySampleRepo` seeding and the recorded asset corpus |
| [`archive/INDEX.md#2d-graphics-debug-api`](archive/INDEX.md#2d-graphics-debug-api) | shipped | `@vn/debug2d` — fragment IR, DOM adapter, query engine, `explainPick` |
| [`archive/INDEX.md#interaction-model`](archive/INDEX.md#interaction-model) | shipped | `Interaction`/`targets` and the `interaction.*` commands; five gestures declared. Its two "Next" items shipped in [`archive/INDEX.md#preconditions-and-timeline-interaction`](archive/INDEX.md#preconditions-and-timeline-interaction) |
| [`archive/INDEX.md#allocated-line-ids`](archive/INDEX.md#allocated-line-ids) | shipped | Line ids that survive an edit, the diagnostics surface, the catalog-driven palette |
| [`archive/INDEX.md#lossless-scene-serialization`](archive/INDEX.md#lossless-scene-serialization) | shipped | `parse(write(scene)) ≡ scene`; `Scene.body` retired, headings and three line kinds retained |
| [`archive/INDEX.md#scene-chunk-files`](archive/INDEX.md#scene-chunk-files) | shipped | `scenes/<id>.md` replaces the one contended screenplay; `start:` names the entry, front-matter is identity only, both prose patchers retargeted (`screenplay/` still loaded when it shipped; plan 4 retired it) |
| [`archive/INDEX.md#fountain-import-export`](archive/INDEX.md#fountain-import-export) | shipped | `vngen import` / `vngen screenplay`, the desktop pair, and the retirement of `screenplay/` as an input |
| [`archive/INDEX.md#scene-editing-commands`](archive/INDEX.md#scene-editing-commands) | shipped | Nine `story.*` prose commands over `@vn/scriptedit`, `session.editScene`, the storyboard fallout, `script.moveLine`, and `vnauthor`'s `edit_scene` — one write path for prose, no UI |
| [`archive/INDEX.md#scene-edit-package`](archive/INDEX.md#scene-edit-package) | shipped | The scene-edit rules and write path now live in `@vn/scriptedit` (pure barrel; `@vn/scriptedit/write` for the filesystem half), where `vnauthor` can reach them |
| [`archive/INDEX.md#line-editing-in-floor`](archive/INDEX.md#line-editing-in-floor) | shipped | Retyping a line in the coverage timeline, and `Shot.proseHash` → the drift mark on the shot it produced |
| [`archive/INDEX.md#script-composition-in-studio`](archive/INDEX.md#script-composition-in-studio) | shipped | STUDIO's `script` mode — a column that writes, inserts, deletes, reorders and attributes lines, the confirmed acts that change which scenes exist, clickable diagnostics, and the end-to-end pass: a scene written in the app, generated, watched in PLAY |
| [`archive/INDEX.md#task-failure-visibility-and-retry`](archive/INDEX.md#task-failure-visibility-and-retry) | shipped | `Task.error` so a failed task records why, a bounded retry on the next run, a run report derived from the live plan instead of claiming success over a failure, and in-place backend retry for transient provider errors |
| [`archive/INDEX.md#portrait-overlay-opt-in`](archive/INDEX.md#portrait-overlay-opt-in) | shipped | The shot is the whole picture: `portrait_overlay` (default off) → `story.play.json`'s `portraitOverlay`, so PLAY stops staging a second copy of the speaker over a frame that already contains them |
| [`desktop-editors-tracking.md`](desktop-editors-tracking.md) | — | Not a plan; the tracker for the six desktop-editor plans above |
| [`refactorTaskList.md`](refactorTaskList.md) | — | Not a plan; the master tracker for the [`../history/designRequirementsEtc.md`](../history/designRequirementsEtc.md) refactor — the path.ux UX rewrite and the structural plans the [migration report](../research/codebase-migration-for-new-requirements.md) calls for |
| [`archive/INDEX.md#entity-discovery-by-meta-tag`](archive/INDEX.md#entity-discovery-by-meta-tag) | shipped | Characters/locations found by front-matter `type:` tag across `characters/`, `locations/` and `wiki/**`; `EntityDoc` carries the source path so no writer re-derives one; id/filename agreement and duplicate diagnostics |
| [`archive/INDEX.md#story-bible-and-retrieval`](archive/INDEX.md#story-bible-and-retrieval) | shipped | The `wiki/` tree read as prose and reached by retrieval: a new `@vn/bible` between store and authoring, `query(text) → ranked excerpts` under a hard budget, `search_bible` and `bible.search` — grep now, embeddings behind the same seam |
| [`archive/INDEX.md#repo-map-and-commit-on-save`](archive/INDEX.md#repo-map-and-commit-on-save) | shipped | Which repo owns a path (`RepoResolver`, discovered not declared), every act committing to each repo it touched, undo restoring as a *new* commit rather than a reset, and the `git init` + commit-existing half of project bootstrap |
| [`archive/INDEX.md#base-and-project-asset-stores`](archive/INDEX.md#base-and-project-asset-stores) | shipped | Base art (portraits, model sheets, location refs) split into its own content-addressed root at `assets/` — own subtree, own manifest, optionally its own repo — with reads unioned across both roots, an `unavailable` base that refuses to plan rather than regenerating, and `Asset.satisfies` grown to a list of bindings |
| [`archive/INDEX.md#shot-ordering-in-scenes`](archive/INDEX.md#shot-ordering-in-scenes) | shipped | Reordering shots inside a scene as what it actually is — moving the shot's covered lines — with the refusal that makes it definable (a shot with interleaved coverage has no single position), `moveShot` in `@vn/scriptedit`, `story.moveShot`, the `timeline.reorder` gesture that previews it, and `edit_scene`'s tenth op |
| [`archive/INDEX.md#outfits-at-scene-and-shot-level`](archive/INDEX.md#outfits-at-scene-and-shot-level) | shipped | An authorable wardrobe (`outfits:` on the character sheet) and an inheritance chain — shot override → `[[outfit: aiko=uniform]]` scene marker → character default — resolved by one `outfitFor`, with a non-default outfit rendered against its own model sheet and P4's fan-out narrowed to the outfits a reachable scene actually asks for |
| [`archive/INDEX.md#agent-context-regeneration`](archive/INDEX.md#agent-context-regeneration) | shipped | One generated `AICONTEXT.generated.md` — the cast, the locations, the story graph and the bible's *table of contents* — written by `workspace.reindex` off the two walks that already exist, read one rung below the author's own `AICONTEXT.md`, budgeted, and refusing to overwrite a file it did not write |
| [`archive/INDEX.md#document-tree-and-backlinks`](archive/INDEX.md#document-tree-and-backlinks) | shipped | The sidebar's two shapes, joined from edges that already exist: a document tree (story → scenes → shots, characters, locations, the wiki tree, assets by kind) plus per-entity backlinks (sheet, base art, scenes, shots), on their own channel so the hot workspace index stays cheap |
| [`archive/INDEX.md#project-bootstrap-and-workspace-picker`](archive/INDEX.md#project-bootstrap-and-workspace-picker) | shipped | The other half of project bootstrap: a picked directory becomes a project (`openWorkspace` writes a one-line `project.yaml`, then `ensureRepo`), `workspace.open`/`pick`/`recent`, an in-place switch that tears the session and undo stack down with the old root, and a startup precedence that remembers the last project |
| [`archive/INDEX.md#wiki-and-document-tree-editors`](archive/INDEX.md#wiki-and-document-tree-editors) | shipped | The panes for the story bible and the document tree: a `documents` sidebar (logical tree + file-tree mode + backlink panel + a New… row), a `wiki` markdown editor, and the `doc.read`/`doc.write`/`doc.create` commands they need — the UI items 3 and 9 each deferred to the other. Eleven decisions, five of them found by auditing the draft against the code |
| [`archive/INDEX.md#pathux-desktop-rewrite`](archive/INDEX.md#pathux-desktop-rewrite) | shipped | The renderer rebuilt on path.ux (submodule): subdividing screen, seven editors ported cheapest-first, one selection, per-area keymaps, `view.*` addressing editors instead of rooms, and the docs reorganized by editor. The React shell, the `--react` flag and `react`/`react-dom` are deleted; the pure rule modules moved to `renderer/rules/` |
| [`archive/INDEX.md#desktop-shell-fit-and-finish`](archive/INDEX.md#desktop-shell-fit-and-finish) | shipped | Eight gaps collected while using the path.ux shell for real: a window that would not close (`will-prevent-unload`), the stock menu deleted with Ctrl+Q and F12 re-homed, a bounded dialogue box, the agent's mode and a working indicator on the convo pane's own bar, model + effort pickers (`agent.setEffort`, `TEXT_MODELS`/`EFFORT_LEVELS` moved to `@vn/types`), and an `onWrote` bus so an open document follows the agent's writes — plus a `⟳` on both editors |
| [`archive/INDEX.md#asset-names-and-the-asset-editor`](archive/INDEX.md#asset-names-and-the-asset-editor) | shipped | Generation from an authored surface: assets named rather than hashed in the document tree, `artNotes` as an authored field at five rungs (character, location, outfit, variant, shot) appended to the derived prompt so an edit re-keys the task and re-renders exactly what it reaches, the `asset.*`/`art.*` commands, and the eleventh editor — which previews one asset, approves it, and regenerates it from where the tree names it |
| [`archive/INDEX.md#on-demand-concept-images`](archive/INDEX.md#on-demand-concept-images) | shipped | A picture the pipeline never asked for: `@vn/artgen` (prompt composition moved out of `@vn/pipeline` so the agent can reach it), a `concept` asset kind that is bound but never consumed, `generate_image` + `/makeimage` in `vnauthor` and `art.generate` in the desktop, and `art.promote` — which writes the variant onto the location sheet and records the planner's own task as `done`, so the next run adopts the sketch instead of rendering over it |
| [`archive/INDEX.md#desktop-agent-permissions`](archive/INDEX.md#desktop-agent-permissions) | shipped | The desktop answering the agent's other two permission doors: `ask_user` and every always-confirm tool were scaffolded to `''` and `true`, so the app answered for the author — a question card and a confirm card in the convo pane, over the plan card's own request/reply shape, with an English sentence built in main rather than the raw arguments, and teardown resolving every parked door instead of hanging the turn |
| [`archive/INDEX.md#chunked-prompts`](archive/INDEX.md#chunked-prompts) | shipped | The prompt as an addressable list of clauses rather than a flat string: `PromptChunk[]` behind every builder (collapsing byte-identically, so no task hash moves), per-chunk replace/append/mute/reorder, a verbatim user prompt and an agent-condensed one that is *held* rather than re-rendered when its chunks move, a `prompt.*` namespace and a rebuilt asset pane — plus Part II, where a chunk carries reference images: a linked asset pinned by hash and remembering the slot it came from, derived viral suspension when that slot moves, an acyclic reference graph enforced over bindings at write time, custom uploads as a new `reference` kind, and the retirement of the never-read `Character.referenceImages` |
| [`archive/guided-ui-tours.md`](archive/guided-ui-tours.md) | shipped | The agent showing an author how to do something by pointing at it, on an **anchor layer** where a widget's click and the record naming it come from one `Offer`, so the two cannot drift apart. Item keys are domain identity; the registry is generation-scoped and re-resolved on the overlay's own frame loop rather than path.ux's 150 ms one; each half is cross-checked by an oracle that was not consulted when it was built — `stack.check` against what a pane offers, a shadow-piercing hit test against the rect a ring is drawn from. The map is half *derived* from `menuFor` and half *measured* into a committed `anchors.json` by a CDP sweep, with a coverage floor as the CI half. On top of it: `command`/`input`/`select`/`gesture` steps, a `pointer-events: none` ring layer, advancing on `onExec` whatever ran the command, the prefilled palette as the route that always exists, `show_me` for the tail, and the rule that a tour never clicks for you. Pressure-tested by [`../research/pressure-test-guided-ui-tours.md`](../research/pressure-test-guided-ui-tours.md); as shipped, [`../reference/guided-tours.md`](../reference/guided-tours.md) |
| [authoring-surface-tasklist](archive/INDEX.md#authoring-surface-tasklist) | shipped | The running order and checkbox list for the eight plans below, with the only two hard dependency edges named |
| [`archive/INDEX.md#editor-routing-by-relevance`](archive/INDEX.md#editor-routing-by-relevance) | shipped | Clicking a document tree item shows the editor that can best answer for it: claims declared beside the names in `editors.ts` as predicates over the node, a pure `routeFor()` sorting on `(visible, tier, EDITORS order)`, selection published before the open, and `where: 'elsewhere'` as the fallback that already exists |
| [`archive/INDEX.md#asset-cross-references`](archive/INDEX.md#asset-cross-references) | shipped | A page showing the art that references it: `scene:<id>` backlink keys and a path index on `DocTree`, and the asset strip extracted into a generic widget with two consumers — the wiki editor and the script editor — plus the honest finding that no asset binds to a plain lore note today |
| [`archive/INDEX.md#new-and-open-project`](archive/INDEX.md#new-and-open-project) | shipped | `workspace.create` beside `open`/`pick`/`recent`: a three-file skeleton so a new project loads a model with zero error diagnostics, a refusal on a non-empty directory, a warning when the target sits inside an existing repo, and the New/Open/Recents menu set |
| [`archive/INDEX.md#new-project-dialog-with-folder-browse`](archive/INDEX.md#new-project-dialog-with-folder-browse) | shipped | `prop.directory` and the palette's Browse… button, `workspace.chooseDirectory`, and a `newFolder` checkbox that makes `workspace.create`'s form a real New Project dialog |
| [`archive/INDEX.md#new-project-as-its-own-dialog-and-its-own-repo`](archive/INDEX.md#new-project-as-its-own-dialog-and-its-own-repo) | shipped | Two faults the author found in the one above: the form extracted into a `CommandForm` with two hosts, so the palette stays the finder and a named command gets its own dialog with Cancel and no search box; and `initRepoAt`, `ensureRepo`'s opposite, so creating a project inside a repo nests one of its own instead of silently getting none |
| [`archive/INDEX.md#conversation-threads`](archive/INDEX.md#conversation-threads) | shipped | Conversations saved as append-only JSONL under `vngen/state/threads/` — outside the undo snapshot by design — with the reducer moved to `src/shared/` so main and the renderer agree, a searchable dropdown, and reopening that replays read-only rather than pretending the model remembers |
| [`archive/INDEX.md#upload-and-archive`](archive/INDEX.md#upload-and-archive) | shipped | `/upload`: an author's documents archived verbatim under `archive/`, invisible to `search` and to entity discovery because both walk allow-lists and readable by name, with content-blind suggestion chips, a seeded thread in plan mode, and one `archiveUpload` behind both the desktop command and the REPL |
| [`archive/INDEX.md#adopting-an-uploaded-asset`](archive/INDEX.md#adopting-an-uploaded-asset) | shipped | Uploaded artwork becoming a slot's actual output rather than a reference: `adoptSlot` generalized out of `promoteConcept`, addressed by the existing `plate:`/`sheet:`/`shot:` slot vocabulary, with the planner's input builders extracted so hashes cannot drift, a portrait refused because the P3 gate owns it, superseding a real render as a declared act, and an asset-editor Replace strip that reads the slot off the picture on screen rather than asking for one |
| [`archive/INDEX.md#agent-art-revision`](archive/INDEX.md#agent-art-revision) | shipped | The agent reaching planned art: the art-notes rungs moved to `@vn/artgen` so one parser serves both hosts, regeneration as an injected `pipeline?` capability rather than an argument with the boundaries rule, a general `describeAsset` over the `ChatBackend` seam instead of a widened `VisionReviewer`, and five tools that propose notes rather than silently iterating |
| [`archive/needs-approval-icon-in-the-toolbar.md`](archive/needs-approval-icon-in-the-toolbar.md) | shipped | A toolbar dropdown next to the notification bell listing every asset `session.approvable()` returns — reusing the bell's own `ShellState` badge + IPC push + popup architecture: a persisted per-project ordering (`APPROVAL_ORDER_KEY`) so the most-recently-surfaced batch stays on top across restarts, an unawaited debounced recompute off the `onRecord` hook so a `loadProject()` reload never sits on a routine command's critical path, blocked and settled rows shown with their own reason rather than hidden, and a click that jumps straight to the Asset Editor via the existing `asset` editor's subject wiring |
| [`multiple-windows.md`](multiple-windows.md) | planned | Editors spread across monitors: several `BrowserWindow`s in **one** main process, `win` replaced by a window registry, effects split into broadcast (agent, undo, session) and targeted-at-the-sender (`view.*`, permission prompts), per-window layout and selection keys, a `window.*` namespace and `view.open where='window'` — plus the single-instance lock, because two app instances today write the same `refs/vn/undo/<seq>` snapshots |
| [`archive/INDEX.md#document-tree-context-menus`](archive/INDEX.md#document-tree-context-menus) | shipped | The first context menus in the app, built from the catalog: entries are invocations resolved through `check` then `exec`, a refused entry shown with the command's own sentence rather than hidden, `undeclared` explicitly not permission, and one table per node kind including the kinds that offer nothing |
| [`archive/INDEX.md#notifications`](archive/INDEX.md#notifications) | shipped | One durable, linkable notification log in the project repo — `vngen/state/notifications.jsonl`, per-line versioned, union-merged, with read/hidden as single ASCII digits patched in place at a byte offset — filed for every command outcome from one `onRecord` hook, narrowed to a single note frame in the menu bar, and read through a bell, a scrollable list with in-place archive/undo, and a category filter |
| [model-keys-tree-menus-and-inline-rename](archive/INDEX.md#model-keys-tree-menus-and-inline-rename) | shipped | Seven todos taken together: a `secret` prop kind so `project.setKey` can write a credential the history records as `<secret>`, a `.gitignore` written before a new repo's first commit so `keys` is ignored by the time anything can be committed, branch headings offering what their subtree is made of, a capture-phase pointer-down latch so the click that dismisses a menu cannot collapse the tree underneath it, and double-click-to-rename over a `doc.rename` that writes wherever the name was read from and never moves the file |
| [`provider-credentials-and-the-ai-usage-ledger.md`](provider-credentials-and-the-ai-usage-ledger.md) | planned | Every author billed to their own account, and a truthful account of what that cost: the finding that only Vertex offers real third-party OAuth (and works by storing nothing), so the work is in making key handling good rather than building an OAuth client; usage widened onto the response where it already exists and thrown away today; the rule that every *billed* call is recorded including the retry-discarded ones; a `vngen/state/usage.jsonl` ledger in integer micro-dollars, counts-only because it is committed; the thirteenth editor; a header badge showing spend rather than a balance no provider exposes; and the seven faults in `project.setKey` behind the "add a dropdown" todo — starting with a `secret` prop that renders in plaintext and is shipped over IPC on every keystroke |
| [`archive/INDEX.md#deliberate-reasoning-effort-defaults`](archive/INDEX.md#deliberate-reasoning-effort-defaults) | shipped | `default` removed from the effort menu, because on Opus 4.7/4.8 and Sonnet 4.6 "the knob left off" runs the model with no thinking at all: a per-model table in `@vn/types` says what each one's ladder is and whether `no thinking` is even offerable (Fable 400s on it), `low` becomes the stated default everywhere including the pipeline's own calls, `resolveEffort` steps a stored choice down when the model switches under it, and the no-thinking `max_tokens` goes 2048 → 10000 |
| [`archive/INDEX.md#reporting-a-difficult-agent`](archive/INDEX.md#reporting-a-difficult-agent) | shipped | Help ▸ Report a Difficult Agent…: the finding that a thread is a display log rather than an execution one (tool args and results dropped, every text clamped at 400) so the format is enriched first and old threads are joined against `commands.jsonl` by time instead; a debug agent on the author's own key and their own machine, given the app's own source through a declared manifest and an allow-listed `fetch_api_docs` rather than an arbitrary fetch, because an agent that has just read a private manuscript is otherwise an exfiltration channel; de-personalisation as a boundary every byte crosses rather than a prompt instruction, with the same matcher run back over the finished report as a live refusal; the analysis borrowing the bound model and effort without rebinding them, advising rather than refusing because the verdict strip is binary and `agent.setEffort` already accepts every choice; and a GitHub issue the author reviews and submits themselves, the URL asserted before it reaches the shell |
| [`archive/INDEX.md#diagnosing-an-api-error-from-the-request-that-caused-it`](archive/INDEX.md#diagnosing-an-api-error-from-the-request-that-caused-it) | shipped | A 400 that names a position is unreadable once the body is gone, so the body stops being gone: an always-on in-memory ring in `@vn/providers` bounded by bytes *and* count (64 MB / 64 entries), every Anthropic and Gemini call site capturing its vendor body before the call rather than after it, `faultKind` unwrapping `cause` so a fault in the request is told apart from a dead connection and a bad key, the recovery card offering to look into the first kind only, a `UiEffect` that opens the difficult-agent dialog by itself with both reading boxes ticked and a sentence saying why, `list_requests` / `read_request` giving the analyst structure and capped redacted values by JSON Pointer rather than a pasted body, `analyze.ts` split so the requests are a door the source is not — and the rule that none of the capture ever reaches the report, so the detailed tier's privacy area is exactly the author's own model provider; verified against a loopback fake API through the real SDK, with the analyst told apart from the agent by its own `submit_report` tool |
| [`archive/INDEX.md#layout-templates-and-the-view-menu`](archive/INDEX.md#layout-templates-and-the-view-menu) | shipped | The View menu's editor list folded into an Editors submenu to make room for Layout: named arrangements the project owns at `.vnstudio/layouts/`, carrying either a declarative recipe (so main can ship, scaffold and reset them with no renderer in the loop) or a serialized mesh (so an arbitrary dragged layout round-trips), a `-merge` attribute that makes git conflict one rather than invent a third, an undo that restores the file *and* the screen by noticing the fingerprint moved, and `digest` props finally rendering as a summary instead of a 21 KB textbox |
| [`archive/INDEX.md#the-full-slot-graph-and-approving-upstream-first`](archive/INDEX.md#the-full-slot-graph-and-approving-upstream-first) | shipped | Every picture the project implies, not just the wave the planner could hash: a slot graph over `refsOfSlot` with the planner's enumerators lifted into `@vn/model` so the two cannot drift (and a test that plans a project to exhaustion to prove it), prerequisites walked over `Asset.refs` so a listed row is clickable, an Approve greyed by its own refusal with the same sentence `asset.accept` gives, a DRAWN FROM strip that retargets the pane in place, an Unapproved branch in two groups the slot graph makes disjoint, `story.decomposeAll` / `vngen decompose` that storyboard every reachable scene and never persist a fallback, and the death of the graph pane's estimated ghost clusters |
| [`fast-mode-as-an-opt-in.md`](fast-mode-as-an-opt-in.md) | planned | Anthropic fast mode as a per-conversation toggle that is off by default and never inferred: a `supportsFastMode` allow-list of exactly Opus 5 and Opus 4.8 (written as an enumeration because 4.7 had it and lost it, so the supported set has a hole in the middle), the beta endpoint plus `betas` plus a top-level `speed` sent as one gated block, a bound `fast` kept across a model switch and simply not sent, a 429 retried at fast speed and never silently downgraded because switching speed invalidates the prompt cache and re-bills the whole prompt, `usage.speed` carried out as the receipt, and the doubled rate stated in the tooltip in both the enabled and the greyed state |
| [`archive/INDEX.md#prompt-caching-and-deferred-tool-loading`](archive/INDEX.md#prompt-caching-and-deferred-tool-loading) | shipped | Why a small project bills ~376k input tokens for one conversation, and the five workstreams that fix it: cache reads and writes split out of `TokenUsage` so the rest can be believed, a conversation-shaped `chatConversation` request with four `cache_control` breakpoints (tools, system, and two rolling ones in `messages`) replacing the one re-rendered string, tool arguments in the transcript so the agent stops reading back what it just wrote, `MODE` moved out of the prompt prefix into a `{"role":"system"}` message, the desktop switched off `StructuredAgentBackend`, and the native tool search tool + `defer_loading` instead of a hand-rolled `load_tool` — because appending a schema to `tools[]` invalidates the whole prefix and the server-side version does not |
| [`archive/INDEX.md#the-todos-sweep`](archive/INDEX.md#the-todos-sweep) | shipped | The author's running list worked through in one pass: the run-pipeline dialog and its busy icon, the recent-projects menu, popup borders and word wrap, a key-provider dropdown, the agent's running token total, the character template, tooltips on every menu entry, and the several tree and script-editor bugs the list named |
| [`archive/INDEX.md#drawing-a-character-before-a-scene-casts-them`](archive/INDEX.md#drawing-a-character-before-a-scene-casts-them) | shipped | P2/P3 enumerate every authored location and character rather than only what a reachable scene names, so a cast sheet gets its portrait before a scene exists to put it in; P4 stays tied to `usedOutfits`, and `gateStatus` keeps its own walk so an uncast portrait blocks nothing |
| [`archive/INDEX.md#improving-the-authoring-agent`](archive/INDEX.md#improving-the-authoring-agent) | shipped | What the adversarial read of `examples/test4` found, built: a per-turn **token budget** replacing the step count (checked between steps, warning the agent at 80%, naming the spend and the uncommitted files when it runs out), an **`edit_file`** that replaces exact strings against a per-conversation read ledger so a long document is changed in part rather than restated, a bulk `insertLines`, create tools that take the edit tools' whole field set, tool descriptions and observations that tell the truth about their own scope, six system-prompt edits, and the host plumbing that puts plans, verdicts, shortlists and refused arguments into the durable thread `report.agent` reads |
| [`archive/INDEX.md#superseded-assets-in-the-document-tree`](archive/INDEX.md#superseded-assets-in-the-document-tree) | shipped | The Assets branch asks the slot graph which take is current and files the rest under a collapsed `Superseded` child — undecided candidates all count as live, and an asset no slot mentions stays put |
| [`archive/INDEX.md#shipping-the-app-tasklist`](archive/INDEX.md#shipping-the-app-tasklist) | shipped | The five plans between "it runs from a checkout" and "a stranger has it installed and updating", their running order, and the three real edges between them |
| [`archive/INDEX.md#onboarding-editor-and-user-level-keys`](archive/INDEX.md#onboarding-editor-and-user-level-keys) | shipped | API keys above the project — a platform-native user config dir with `$VNAUTHOR_HOME` and a `~/.vnauthor` fallback read, appended as a fourth rung so a project's own key still wins; `project.setKey` gaining a scope and `project.keyStatus` naming the source without the value; a thirteenth `offered: false` editor walking an author through both vendors' consoles with a Test key button; and the path.ux addition that makes it possible — `setAreaMenuFilter`, because `AreaFlags.HIDDEN` is static per class and which editors an app offers is the app's policy |
| [`archive/INDEX.md#packaging-the-desktop-app`](archive/INDEX.md#packaging-the-desktop-app) | shipped | electron-builder into an NSIS installer: what ships (only `dist/` plus the two SDKs `EXTERNAL` leaves unbundled, and never `pathux-types`), the hoisted install that keeps pnpm's symlinks out of the app image, `git` declared a runtime dependency with a startup doctor rather than a bundled copy, and `apps/desktop/package.json` named the one version a tag must match |
| [`archive/INDEX.md#release-ci-workflow`](archive/INDEX.md#release-ci-workflow) | shipped | The two workflows this repo has never had: per-push check/test/lint with `submodules: recursive`, and a tag-triggered release that gates once, builds a matrix, asserts the tag against the version rather than writing it, and leaves a **draft** for a person to publish |
| [`archive/INDEX.md#skills-editor-and-agent-authored-skills`](archive/INDEX.md#skills-editor-and-agent-authored-skills) | shipped | Skills made visible and writable: a Skills branch in the document tree that is drawn even when empty, because a created project has no `.aiagent/` at all and an absent branch teaches nobody; a thirteenth editor over every file under `.aiagent/skills` with a hint and a button that hands the author to the agent; `create_skill`/`edit_skill` with a `write_file` gate that makes the agent's skills **prose only**, since a confirm card cannot tell a script a human vetted last year from one the agent wrote ninety seconds ago; and two extractions the second host forces — the tree renderer with its counted double-click, and the document buffer with its draft map and content-hash refusal. Pressure-tested: [`../research/pressure-test-skills-editor-plan.md`](../research/pressure-test-skills-editor-plan.md) |
| [`archive/INDEX.md#in-app-update-checks`](archive/INDEX.md#in-app-update-checks) | shipped | `app.checkForUpdates` as a registered command reading the releases feed and comparing semver — check-only first because `electron-updater` on macOS needs signing and fails silently without it; the notification rather than a modal, the browser hand-off, quiet failure, and a periodic check off by default |
| [`archive/INDEX.md#auditing-the-api-key-instructions`](archive/INDEX.md#auditing-the-api-key-instructions) | shipped | Keeping `docs/api-keys.md` true: a per-vendor yaml block so the facts are machine-readable, a deterministic blocking link check because a moved console is the failure that actually happens, and a weekly **advisory** semantic audit that opens an issue and never edits the file a confused new user is reading |
| [`code-signing-and-notarization.md`](code-signing-and-notarization.md) | planned | Deferred by decision, written down so it is not rediscovered: unsigned Windows is a dismissible papercut and unsigned macOS is a wall, what the three options cost, and the three triggers that make it worth doing |
| [`archive/INDEX.md#gemini-estimated-cache-hit-rate`](archive/INDEX.md#gemini-estimated-cache-hit-rate) | shipped | Gemini's implicit cache reported in the tokens tooltip: `cachedContentTokenCount` carved out of `input` as `cacheRead`, a `cacheEstimated` flag riding the same wire so a matched-prefix count is never shown as a bill, the tooltip prose moved out of the editor into a tested `tokensDetail`, and a five-call live ritual — because the implicit cache is slow to start and then intermittent, so a two-call proof fails a working implementation |
| [`four-chat-vendors-and-two-more-image-providers.md`](four-chat-vendors-and-two-more-image-providers.md) | planned | Anthropic, Gemini, OpenAI and Grok behind one `ChatBackend`, and OpenAI Images + BFL FLUX.2 behind one `ImageBackend`: an ordered vendor table in `@vn/types` replacing `chatVendorFor`'s two-branch string test and its two duplicates, `ResolvedKeys` widened from a pair to a record so the key dialog grows by itself, a neutral `ChatBlock` vocabulary (text / toolCall / toolResult / **opaque**) that gets Anthropic wire shapes out of `@vn/authoring` and stops a cross-vendor model switch shipping one vendor's reasoning blocks to another, caching worked out per vendor with no shared abstraction because the four disagree on what a cached prefix *is*, the `{"role":"system"}` append trick becoming a developer message on OpenAI and the existing down-render on Gemini, the two mirror-image usage traps (OpenAI's `input_tokens` already includes the cache read; its `output_tokens` already includes reasoning, where Gemini's does not), and the refusal that decides the image half — `gpt-image-2` has no seed, so a seeded request is refused by name rather than silently drawn |
| [`archive/INDEX.md#what-the-agent-knows-about-the-story-format`](archive/INDEX.md#what-the-agent-knows-about-the-story-format) | shipped | The authoring agent's knowledge of the story format, routed to the three places it can live: `SYSTEM_PROMPT` gains all six `BranchMarker` kinds with `[[line:]]`/`[[nextline:]]` marked machine-managed, a closed-world sentence saying branching is scene-granular and nothing finer, and a preamble that stops denying `approve_assets`; `planSceneEdit` starts refusing a write that would drop a note the model cannot hold, because today the first unrelated edit to a scene silently deletes the author's `[[TODO:]]`; the validator then reports the same notes, split by consequence rather than by confidence (`unparsed_branch_marker` error where an edge or a shot anchor is lost, `unknown_marker` warning where `branch.ts` documents a plain-note fallback), joining `droppedWarnings` as the fourth thing the model does not keep; one new skill, `branching`; plus `deleteLines` and a `writeFileAtomic` temp name that is random and cleans itself up. Decides the budget's units (characters, asserted in a test that refuses rather than truncates), and records three things already true that the reports said were not — `git_commit` has gated on error severity since `883d4a25`, a chunk holding two scenes is caught by `entities.ts`, and only `vngen run` refuses on an error while `export` proceeds. |
| [`archive/INDEX.md#publishing-a-vn-to-github-pages`](archive/INDEX.md#publishing-a-vn-to-github-pages) | shipped | A generated VN publishes to the web as a light novel: `renderSite` projects the playable into one static HTML page per scene with `choices` and `next` as links and no prose rewriting, VN STUDIO ▸ Install/Update GitHub Page Builder… commits a dependency-free bundle of that renderer into the author's project beside a workflow that runs it with plain `node` (every package here is `private: true`, so CI cannot install one), and the workflow force-pushes an orphan `gh-pages` branch rather than deploying to Pages directly — refusing any existing branch that carries no `.vn-pages` marker. The app commits and never pushes |
| [`creating-shots-by-hand-and-by-agent.md`](creating-shots-by-hand-and-by-agent.md) | planned | Shot creation and coverage assignment become authorial acts instead of `decomposeScene`'s monopoly: the coverage rules move from the desktop's `shared/` into `@vn/scriptedit` beside `newShot`/`deleteShot`, `story.newShot` is offered from the Coverage editor's gap gutter (a hand-made first shot ends the "decompose me" signal the same way P5 does), the strip shows an in-flight command as a progress bar and locks the gesture surface with a sentence instead of going silent, and the agent gains `set_coverage`, the two shot ops, and `propose_storyboard` — decomposition as a plan the author approves before anything is persisted |
| [`archive/INDEX.md#watching-and-stopping-an-agent-report`](archive/INDEX.md#watching-and-stopping-an-agent-report) | superseded | Superseded by [`archive/INDEX.md#the-debug-agent-as-a-conversation`](archive/INDEX.md#the-debug-agent-as-a-conversation), which carries its busy-state work and its fallback fix and answers the rest with a transcript and a composer rather than a progress popup. The longest thing the app does on the author's key stops being silent: the report's step count fed into the busy push that already broadcasts to every window, the header's spinner and stop button generalised off the one busy kind they are hardcoded to, a cooperative `report.stop` on its own `AbortController` (and the refusal that says a single-call analysis has no step to stop after), a progress popup showing tool names and spend but never unscrubbed model prose, and the fallback bug a stop exposes — `analyze()` treats a stopped loop as one that wandered off, so pressing Stop would spend another model call and hand back a report nobody asked for |
| [`archive/INDEX.md#prompt-caching-for-the-report-analyst`](archive/INDEX.md#prompt-caching-for-the-report-analyst) | shipped | The report analyst joins the cached path: `analyzeWithTools` probes `chatConversation` the way `session.ts` and `vnauthor` already do, taking `NativeAgentBackend` (rolling breakpoints, `record: false` preserved through the convo capture) on Anthropic models and falling back to the structured path everywhere else, plus a `deferTools: false` agent option because on the native path the default deferral would hide `submit_report` itself behind tool search — the direct single-call path deliberately stays uncached |
| [`archive/INDEX.md#the-debug-agent-as-a-conversation`](archive/INDEX.md#the-debug-agent-as-a-conversation) | shipped | The difficult-agent report stopped being a dialog that blocks and became an agent the author talks to: a Debug Agent pane opened as a popup from the Help menu and listed in neither of the two menus an author browses, its own store fed by a `report:event` channel that must never be `agent:event` (which would append the analyst's turns to the very conversation being analysed), the six form props redrawn as the first card in the transcript with `note` becoming turn one, `createAnalyst` holding the loop `analyzeWithTools` threw away so a reply continues it, source and request access granted part way through by growing a registry the next turn's catalog reads and announcing it in a superseding system message, live prose redacted before it is shown so the chat and the report use the same names, a cooperative stop plus the fallback bug it exposes, and ten redacted transcripts kept at `<userConfigDir>/debug-transcripts/` with the oldest pruned |
| [`refine-corrections-tool-schemas-shot-variants-and-commit-subjects.md`](refine-corrections-tool-schemas-shot-variants-and-commit-subjects.md) | planned | Five findings from one `vnauthor` conversation in `examples/test4` and the debug report filed on it: a frame that went through a P7 refine can never be regenerated, because the staleness test compares a derived prompt against the prompt as sent and `refinePrompt` appended a `Corrections:` clause to it — 50 of 306 frames in that project, and the refusal tells the author to run a pipeline that plans nothing; a tool's real schema never reaching the model, since `LOOSE_PARAMS` is sent for every tool and `describeToolParams` flattens `write_storyboard`'s nested shots to `object[]`, which cost four rejected calls in one thread; `Shot.location` being a location *variant* that a plain `z.object` silently strips an alias for, that `realizeDecomposition` coerces to the first variant without saying so, and that no command or tool can change afterwards; an agent's whole chat reply becoming a commit subject because `subject()` never looks for a newline; and the report analyst asserting an author-reported symptom as established fact while recording that it could not corroborate it, then recommending a tool the boundaries rule forbids `@vn/authoring` from ever having |
| [`asset-gallery-popup.md`](asset-gallery-popup.md) | planned | A virtualized, searchable thumbnail grid in path.ux (`AssetGallery`/`AssetGalleryGrid`/`AssetThumb`), so picking a reference asset stops meaning paste a hash into `prompt.addRef`'s `ref` prop: pooled DOM cells recycled on scroll rather than canvas, a global `ThumbnailCache` (LRU, coalesced, `ImageBitmap.close()` on evict), roving-tabindex keyboard nav, a three-state hover/focus/active cell model with separate `"change"`/`"confirm"` events, a full theme spec (background, highlight, active, focus ring, border, margin, padding), and one concrete desktop consumer — an Attach… button in the asset editor's `chunkActs()` |
| [`recording-cache-misses-in-a-thread.md`](recording-cache-misses-in-a-thread.md) | planned | A saved transcript records, per API call, whether the prompt cache was read as it should have been: an optional `cacheReporting` on `ChatBackend` declaring which backends report a bill rather than a guess (the gate against a local runtime's hardcoded `cached_tokens: 0`, which is byte-identical to an honest miss), the four verdicts `cold`/`hit`/`expired`/`miss` computed in `NativeAgentBackend` rather than the loop because `StructuredAgentBackend` sums a retried step's attempts into one incomparable receipt, the five-minute TTL named as a constant so `expired` can be told from `miss`, a fifth `usage` line in the thread file that every existing reader already ignores correctly, and `redactEvidence`'s open spread closed on the way past. Written from [`../research/tracking-cache-misses-in-transcripts.md`](../research/tracking-cache-misses-in-transcripts.md) |
| [`archive/INDEX.md#clustering-the-global-task-graph`](archive/INDEX.md#clustering-the-global-task-graph) | shipped | The task graph editor's overview stops handing `layoutGraph` the raw per-task graph, where one shared portrait fans out to every shot that refs it and blows a rank out to the shot count: `clusteredGraphOf` groups tasks/slots by scene, character and location first, so the layered layout only ever sees a handful of clusters; a searchable slot list opens `subgraphFor`'s ancestor closure of one slot, and clicking a cluster opens `clusterMembers` — both small task-level graphs that keep the existing layout unchanged |
| [`archive/remembering-ui-state-in-the-project.md`](archive/remembering-ui-state-in-the-project.md) | shipped | The arrangement an author left a project in moves out of the install-global `session.json` and into a gitignored `.vnstudio/session.json` beside the layout templates, so it belongs to the directory rather than to the install and survives a rename: the per-window keys drop the `workspaceScope` digest that made the path part of the identity, `isProjectKey` and a `SessionState` router become the one authority over which of the two stores a key lands in (`view.ts` and the window list reach a store directly today, which would have left the template and the window bounds install-global), and the selection grows the two fields `state.ts` refused as machine identity — a saved `assetHash` is written straight back and then repaired by a single `asset.info`, which fails for a deleted asset and names `newerTake` for a superseded one, so no `pipeline:status` is paid at boot. Restored ids are pruned against the workspace index for the project the author edited outside the app, and only while the field still holds what restore put there. A workspace switch reloads its windows instead of rebuilding a mesh under a live one, which is also the fix for the stale scope every window keeps writing today |
| [`node-based-asset-generation.md`](node-based-asset-generation.md) | in progress | Stages 1 to 10 shipped. The node-based asset generator, staged from [`../research/node-based-asset-generation.md`](../research/node-based-asset-generation.md)'s nine settled decisions: `@vn/gengraph` as a constrained leaf holding the node model, hashing, journal, DSL and cost logic over path.ux's own graph module; an executor wired into `createRunners` so a bound graph replaces only the generative step of a task whose identity never moves; `gengraph.*` commands as the only write path for `work/graphs/`; three agent tools editing by whole-DSL replacement diffed by node id; the Gen Graph pane (editor #17) hosting `NodeGraphView` once path.ux stage V2 lands; and plugins — manifest, confirmed install, transpile toolchain, two-layer price tables, and the Gemini providers ported onto the plugin API. As shipped: [`../reference/gen-graphs.md`](../reference/gen-graphs.md) |
| [`archive/resumable-threads-and-compaction.md`](archive/resumable-threads-and-compaction.md) | shipped | Reopening a saved conversation stops being a replay the model is never shown: the backend-native messages get a second file beside the display log (which clamps every text at 400 characters and so can never be replayed verbatim), a `restore` that puts back `messages` **and** the system-prompt sections — because `refreshSystem` on a live transcript files a supersede message per changed section, so restoring messages alone re-sends the whole prompt as messages — and starts the read ledger and the edit set empty, since an empty ledger is exactly what makes the agent re-read a file the author changed outside the app; a resume refused by name when the stored vendor or backend differs from the bound one, because `turnOf` writes Anthropic wire shapes and `renderTranscript` silently drops every tool call; compaction as an act the author asks for, billed to the conversation's own model, appending a `compact` line to both logs and rewriting nothing, cutting only where every call above the cut is already answered; and `search_history` / `read_history` over the messages a summary covered, registered by the host through `AgentOptions.registry` rather than moving `threads.ts` down to `@vn/authoring`, which still has one host. Opens by closing the hole the second file would make durable — `read_file` can read `keys/` today, and a key in a clamped display log nothing replays is a different thing from a key in a committed file a resume sends back to the provider |
| [`archive/group-nodes-in-the-gen-graph-editor.md`](archive/group-nodes-in-the-gen-graph-editor.md) | shipped | Groups in the Gen Graph pane, the desktop half of path.ux's [`group-node-authoring`](../../vendor/path.ux/documentation/plans/group-node-authoring.md) plan: a selection becomes a definition under `work/graphs/lib/` with an instance in its place (Ctrl+G, Edit ▸ Create Group), entering the instance edits the file and every graph instancing it follows; a node inside a group is addressed by an id chain (`3/7`) so hashes, the journal and the executor stop colliding on per-graph ids; every structural command gains a `group` prop naming the definition it edits; the DSL says `group: <ref>` so the agent's whole-graph edit keeps instances; and the pane resolves definitions over a `gengraph:group` channel and syncs per document path. As shipped: [`../reference/gen-graphs.md#groups`](../reference/gen-graphs.md#groups) |
| [`gengraph-editing-cost-tasklist.md`](gengraph-editing-cost-tasklist.md) | — | Not a plan; the tracker for the four plans below, and the breakdown of what one node-property edit actually costs |
| [`archive/deferring-commit-on-save.md`](archive/deferring-commit-on-save.md) | shipped | Commit-on-save batched across a run of consecutive edits instead of one commit per command, in `@vn/commands`: a `defersCommit` opt-in on the command definition, declared by `gengraph.setProp` and `gengraph.moveNodes` and nothing else; a flush before any mutating command that does not defer and before undo and redo (which commit the restored tree with `-A` and would otherwise sweep a pending batch into their own commit), plus a 1500 ms idle timer, a workspace switch and quit; a batched record carrying `commitDeferred` and no `commits`; a batch commit naming the last act and the count under a `Vn-Batch` trailer that hyphenates runs of seqs and leaves the gaps non-mutating commands took; a failed batch kept for the next flush and filed as a durable notification; and — because `exec` overlapped freely by design so a deferring command could be inside `run` while another command's `-A` commit swept its write up — a mutual-exclusion chain over flush → run → commit for mutating commands, which really did narrow the concurrency the stack allowed. Pressure-tested: [`../research/pressure-test-deferring-commit-on-save.md`](../research/pressure-test-deferring-commit-on-save.md). The measurement stage falsified the plan's own premise: the commit is 23% of an edit and the undo journal's two snapshots are 56%, the cost is git's process startup rather than the size of the tree `-A` stages, and the change is worth 20% of the wall clock rather than the bulk of it |
| [`archive/gengraph-node-editor-data-api.md`](archive/gengraph-node-editor-data-api.md) | shipped | The Gen Graph pane stops drawing its own node rows: a `DataAPI` scoped to the graph on screen, installed on the view through `ctx.override({ api })` so two panes on different slugs each answer for their own graph, the view pointed at a real path so path.ux draws an unconnected input's editor on the socket's own row, and `prop.on('change')` as the write seam — which needs no path.ux `GraphEdit` change and catches a write from any widget. Deletes `buildNodeUI`, `valueRow` and the prototype patch that assigns `createUI` onto twelve node classes at module load (a node type registered later gets no rows at all), binds `active` as a checkbox mapped to `setActiveOutput`, reverts the one refusal a bound widget can reach because `_fire` discards return values, and suppresses the reload echo for `setProp` alone through a one-shot skip armed from `onExec`, since `ctx.origin` is main-only and `onInvalidate` passes no arguments. Pressure-tested: [`../research/pressure-test-gengraph-node-editor-data-api.md`](../research/pressure-test-gengraph-node-editor-data-api.md), which killed three premises — `useDataPathUndo` defaults to **on** and its coalescing re-enters the write listener, a subscription over `node.props` would be deaf to exactly the socket rows the plan exists to restore, and the refusal it claimed for unticking `active` does not exist. Opens with a path.ux commit normalizing `ToolProperty.setValue` to store-then-fire, which is silently wrong today for the first vector prop anyone declares. Shipped in five stages, with two corrections along the way: `PropFlags.NO_UNDO` per declaration rather than `view.useDataPathUndo = false`, because `propEditRow` builds its widget before the caller parents the row and the parent walk finds nothing; and `readGraphFile` restamping `uiname`, `description` and `flag` from the node type, because nstructjs serializes a property whole and an existing file draws untooltipped rows on path.ux's undo stack |
| [`archive/document-versions-and-live-reads.md`](archive/document-versions-and-live-reads.md) | shipped | Per-document write versions, so a pane can tell the echo of its own write from a change somebody else made. Main keeps a per-path counter, stamps every write, and broadcasts the stamped versions to every window over `documents:wrote`; a pane records the versions its own writes produced and re-reads only for a higher one, holding off entirely while a write is outstanding, with the write that settles the last of them doing the catch-up comparison. `shouldReload` is the whole rule and is unit-tested. Because a pane no longer re-reads its own writes it has to apply every edit locally, which is safe because both sides run the same `decideGenEdit` and `Graph.add` allocates ids from a counter carried in the serialized struct; a refused write is the one case where the pane holds an edit the file never took, so it marks the pane stale and the next settled write re-reads. Graph reads are answered from a parse held in main behind an `(mtime, size)` check. Found and fixed along the way: `touchesGraph` matched `work/graphs/<slug>.json` while writes report `vngen/work/graphs/<slug>.json`, and its own test carried the same wrong prefix, so every reload had been coming from the coarse invalidate |
| [`archive/precise-write-signals.md`](archive/precise-write-signals.md) | shipped | The re-derivation storm the plan above named as out of scope. `ContentStore.restoreDir` already computed a path for every file an undo wrote or deleted and discarded them, so undo and redo now report `written` like any other write and the coarse `onInvalidate` fallback in the Gen Graph pane is deleted; one `touchesInputs` predicate over the directories `loadInputs` reads, asked by the pane and by main so the two cannot disagree, with `wiki/` in it as a deliberate false positive because an entity sheet is found by its `type:` tag and the bible is the third surface; and the header recounting off `documents:wrote` behind that predicate with a trailing debounce, instead of off the undo effect main broadcasts after every record. Stage 4 overturned its own premise — a load was dominated by waiting rather than parsing (116 ms sequential reads against 23 ms parallel and 13 ms of parsing, on 174 files), so the entity surfaces, scene chunks and bible index read through `pool` instead of being cached, which needed each read to collect its own diagnostics and the scene reader to keep results rather than throw eagerly. `Workspace.index()` ~400 ms to ~148 ms, `loadInputs` ~180 ms to ~43 ms |
| [`archive/guided-tour-resolution-fixes.md`](archive/guided-tour-resolution-fixes.md) | shipped | Five fixes to the guided-tour layer, from a bug review of it. A `wrong-subject` step stops telling the author to press a control that would act on the wrong thing — it rings the row that selects the right subject where one is drawn, and refuses naming the subject where none is, which needs `subsumes` to split the props the anchor holds with a different value from the ones it does not take at all, since a search over every needed value rings the character row for a step's `speaker` and sticks there; the `stack.check` refusal cache cleared on a tour restart and on any successful command, since `askedAs` short-circuits the re-ask and `onWrote` returns early on a write reporting no paths, so a standing refusal never expires — and `stop()` closing a palette it routed, which also fixes `tour.cancel` over a routed step; `checkTour` run in `tour.start` rather than only in `show_me`, injected through `CommandHost` because `commands/index.ts` imports the command, plus the `anchor.id !== undefined` guard that stops a step with no id matching every item anchor; gesture verdicts filtered to open panes; and an `input` step advancing only on a value the author typed. Pressure-tested; records the findings it fixed, the two review findings it rejects, and the `art.*` rungs below the entity that have nothing on screen to point at |
| [`enforcing-prose-style-without-context-poisoning.md`](enforcing-prose-style-without-context-poisoning.md) | planned | A tool that applies [`../reference/proseStyle.md`](../reference/proseStyle.md) to one Markdown file, designed around the fact that a model reproduces the style of the prose it has just read. The premise justifies one mechanism and the plan builds only that: a fresh `messages` array per Markdown block, so contamination cannot accumulate and no call ever receives the whole document. A pressure test killed the three mechanisms an earlier draft stacked on top — the sequential revised-neighbour context bought referent resolution rather than isolation, the whole-document final pass had no stated job and the largest blast radius, and the fact-checker's schema confinement is unbuildable over `extractJson` plus zod, so its output is now an enum and character offsets with no string fields to leak through. Everything else is deterministic: a splitter whose case list comes from a survey of `docs/**` (indented fences inside list items, the 78 generated TOCs, tables, headings left alone because `check:doclinks` gates on their anchors), per-block rewrapping to each block's own original width since `.prettierignore` excludes `docs/**`, and a structural guard counting what these docs are made of. The diff is written to a gitignored file rather than printed, because a unified diff carries the original text and printing one hands it straight back to the calling agent. Divides from `commentlint`, which already checks six markdown files, by proposing rewrites rather than flagging lines |

## Scene authoring

Seven plans that together make a scene an editable document. They come from
[`../research/scene-chunks-as-the-authored-unit.md`](../research/scene-chunks-as-the-authored-unit.md).
The order below is a dependency order, not a preference: each plan's guarantees are what the next
one rests on. **All seven are shipped.** One plan not
in the original seven has been carved out since:
[`archive/INDEX.md#scene-edit-package`](archive/INDEX.md#scene-edit-package), a prerequisite for 5's agent tool.

| # | Plan | Depends on | Why it is here |
| --- | --- | --- | --- |
| 1 | [`archive/INDEX.md#allocated-line-ids`](archive/INDEX.md#allocated-line-ids) ✔ | — | Positional ids re-point every shot when a line is inserted. Nothing may edit prose until ids survive |
| 2 | [`archive/INDEX.md#lossless-scene-serialization`](archive/INDEX.md#lossless-scene-serialization) ✔ | — | Writing a scene back today loses its heading. Nothing may write a scene until the writer is honest |
| 3 | [`archive/INDEX.md#scene-chunk-files`](archive/INDEX.md#scene-chunk-files) ✔ | 1, 2 | One contended screenplay becomes one file per scene |
| 4 | [`archive/INDEX.md#fountain-import-export`](archive/INDEX.md#fountain-import-export) ✔ | 2, 3 | Migrates existing projects in, and keeps the format from being lock-in |
| 5 | [`archive/INDEX.md#scene-editing-commands`](archive/INDEX.md#scene-editing-commands) ✔ | 1, 3 | The only write path for prose. No UI; verified through the palette and CDP |
| — | [`archive/INDEX.md#scene-edit-package`](archive/INDEX.md#scene-edit-package) ✔ | 5 | Not one of the seven. 5's rules lived in the desktop app, and a package cannot import an app — so the agent tool needed them moved first |
| 6 | [`archive/INDEX.md#line-editing-in-floor`](archive/INDEX.md#line-editing-in-floor) ✔ | 5 | Correct a line where you can see the frame it produced |
| 7 | [`archive/INDEX.md#script-composition-in-studio`](archive/INDEX.md#script-composition-in-studio) ✔ | 5 | Write, reorder, split and merge — everything that changes which lines exist |

6 and 7 are siblings and independent of each other; the division is one sentence — **FLOOR edits
a line, STUDIO edits the script.**

### Decisions that span the batch

Recorded here because each was settled once and every later plan assumes it.

- **Line ids are scene-scoped and stay that way.** `${sceneId}:L<n>` is what `Shot.coversLines`
  binds to, so no line can cross a scene boundary and keep its coverage. The batch makes the
  detachment visible (in `splitScene`/`mergeScene`) rather than introducing global ids.
- **No edit to a scene invalidates art — which is why drift has to be reported.** `buildShotPrompt`
  reads neither `coversLines` nor line text (prose reaches only the P7 reviewer spec, which never
  enters a task's `inputs`), so retyping a covered line rehashes nothing and re-renders nothing: the
  frame goes on illustrating words the scene no longer contains. Plan 5 settled this against the
  code, and it reversed the batch's original premise. Every plan that can change prose owes the
  author that sentence before the commit, not a bill.
- **Drift is derived, never stored.** A shot is drifted when a hash of the covered lines' *text*,
  recorded when its image was written, disagrees with the hash those lines produce now — computable,
  self-healing, and correct for edits made through the CLI or by hand. Not the task hash: that is
  precisely the hash prose cannot move. Shipped in plan 6 as `Shot.proseHash` (stamped only when the
  image's bytes are new) and `driftOf`; a shot rendered before the field existed reads `unknown`.
- **One authorial act, one command, one undo point.** No batch edits, no JSON-patch command, no
  buffer diffed to commands on save.
- **Both input formats loaded during the move**, and a project with both was an error. Plan 4
  ended the move: `scenes/` is the only form scenes load from, a leftover `screenplay/` is reported
  rather than read, and the both-present error became a warning — nothing that builds no scenes can
  contend with the chunks.
- **A scene chunk's front-matter is its identity and nothing else** — `scene: <id>`, matching the
  filename, on a closed schema. Heading, location, synopsis, `choices`, `next` and line ids stay
  `[[…]]` markers and Fountain elements in the body, because `splitScenes` already reads them there
  and `sceneToFountain` already writes them back losslessly. It was marked for revisit once 4–7 had
  shipped, against working editors rather than ahead of them.
  [`archive/INDEX.md#scene-chunk-files`](archive/INDEX.md#scene-chunk-files) records the argument
  on both sides. **The revisit has happened and came out the same way**: scene-level outfits were
  the first field to want in, and
  [`archive/INDEX.md#outfits-at-scene-and-shot-level`](archive/INDEX.md#outfits-at-scene-and-shot-level)
  took the `[[outfit:]]` marker, because `vngen screenplay`/`vngen import` round-trip markers for
  free and would silently drop a front-matter field. What would change the answer: a second field
  that has no reading as a marker (something positionless *and* structured, e.g. a per-scene
  render budget), or the Fountain projection being retired — neither is true today.

### Blockers found while planning

Things that are broken or dead today and that a plan above has to deal with. Each is scoped into a
plan; the ones marked **fixed** have shipped with the plan that owned them.

- ~~`headingFor` (`packages/model/src/serialize.ts:60`) reconstructs every heading as
  `INT. <LOCATION> - DAY`, discarding `EXT.` and the time of day~~ → **fixed** in plan 2:
  `Scene` carries `headingPrefix` and `locationVariant`, and `headingFor` is gone.
- ~~`currentSpeaker` (`packages/model/src/scenes.ts`) is cleared only on `flush()`, and the
  `action`-with-speaker branch describes a case `parseFountain` cannot produce~~ → **fixed** in
  plan 2: attribution ends with the dialogue block, and `'action'` is no longer a `SceneLine.kind`.
- ~~`buildModel` takes the entry scene as `sceneList[0]` (`packages/model/src/build.ts:154`), which
  becomes readdir order the moment scenes are files~~ → **fixed** in plan 3 step 6: `entry` comes
  from `config.start`, and a missing or dangling `start:` is an error diagnostic rather than a
  fallback to sorted-first.
- ~~Four call sites duplicate `loadInputs` → `parseFountain` → `buildModel` (CLI, desktop session,
  authoring workspace, testkit)~~ → **fixed** in plan 3 step 1: `@vn/model`'s `modelFromInputs` is
  the one sequencing point, and `LoadedInputs` moved to `@vn/parse` so the reader and the builder
  name one shape. Not `loadProjectModel` in `@vn/store` as planned — store may not import `model`.
- ~~`ProjectPaths.sceneFile` / `writeSceneFile` are dead, and they hold the name authored chunks
  want~~ → **fixed** in plan 3 step 2: both deleted, so the name is free for authored chunks.
- ~~`vngen export` and `story.export` already mean the playable, so Fountain output needs a
  different name~~ → **fixed** in plan 4: the Fountain projection is `vngen screenplay` /
  `story.screenplay`, and the two artifacts never share a name.
- ~~`Timeline.tsx:156` refuses to draw an undecomposed scene, which is exactly the scene you want to
  write before paying for art~~ → **fixed** in plan 6 step 2: the script column renders on its own
  with a note, and the vermilion gap gutter waits for a decomposition rather than marking every line.
- ~~`write_file` (`packages/authoring/src/tools.ts:408`) is an unvalidated whole-file overwrite
  that would happily write a chunk with duplicate line ids~~ → **fixed** in plan 5 step 6:
  `write_file` refuses `scenes/` outright and its description names `edit_scene` instead.

### Checklist

- [x] 1 — allocated line ids
- [x] 2 — lossless scene serialization
- [x] 3 — scene chunk files
- [x] 4 — Fountain import and export
- [x] 5 — scene editing commands
- [x] 6 — line editing in FLOOR
- [x] 7 — script composition in STUDIO

## Keeping this file true

- A plan states its own status in its first lines (`Status: **planned**` / a `## As shipped`
  section). This table is a projection of those — if the two disagree, the plan wins and the table
  is stale.
- Update the row in the same commit that finishes the plan, alongside the plan's As-shipped
  section and the `CLAUDE.md` edits its final step calls for.
