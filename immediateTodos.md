[ ]: Make sure all apps have a script entry in the root package.json to start them
[ ]: When the resolve gate dialog says 'No generated portraits yet — run the pipeline first.' there should be a button to run the pipeline to generate just the needed assets.
the resolve dialog should also let you edit what's being gated.
[ ]: Clear the default main menu bar, add 'file' 'edit' and 'view' menus.
[ ]: Add 'exit' to the file menu
[ ]: Add 'provide model key' to the file menu, it should pop up a dialog
     to provide the model key with a dropdown box for the model provider.
