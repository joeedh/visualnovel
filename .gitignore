.claude/worktrees
keys
node_modules
# The desktop app's scratch workspace, seeded from examples/sample on first launch and given
# its own git repo. Generated output belongs there, not in this tree.
examples/mySampleRepo
dist
.turbo
.vndesktop
coverage
*.tsbuildinfo
*.tmp-*
# Bundled entry points the build scripts require() and delete; an aborted run leaves one behind.
.record-entry.cjs*
.DS_Store
